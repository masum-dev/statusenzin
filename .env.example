# =============================================================================
# StatusEnzin — Root Environment Configuration (for docker compose)
# Copy to .env and fill in real values before running: docker compose up -d
# =============================================================================

# ---------- Database ----------
DB_USER=statusenzin
DB_PASSWORD=CHANGE_ME_TO_A_STRONG_PASSWORD
DB_NAME=statusenzin

# ---------- API Port (host-mapped) ----------
API_PORT=8080

# ---------- Web Port (host-mapped) ----------
WEB_PORT=3000

# ---------- Frontend build arg (public API URL the browser calls) ----------
# For local Docker:  http://localhost:8080
# For production:    https://api.yourdomain.com
NEXT_PUBLIC_API_URL=http://localhost:8080

# ---------- JWT Authentication ----------
# Generate a long random string, e.g. openssl rand -hex 32
JWT_KEY=CHANGE_ME_TO_A_LONG_RANDOM_STRING
JWT_ISSUER=StatusEnzin
JWT_AUDIENCE=StatusEnzinApp

# ---------- Platform Super Admin ----------
# This account is auto-created on first boot
ADMIN_EMAIL=admin@yourdomain.com
ADMIN_PASSWORD=CHANGE_ME
ADMIN_FULL_NAME=Admin
ADMIN_TENANT_NAME=Default

# ---------- CORS ----------
# Comma-separated origins allowed to call the API
# For local Docker:  http://localhost:3000
# For production:    https://app.yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# ---------- Stripe (leave empty to use mock mode) ----------
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRO_MONTHLY_PRICE_ID=
STRIPE_BUSINESS_MONTHLY_PRICE_ID=
STRIPE_PRO_ANNUAL_PRICE_ID=
STRIPE_BUSINESS_ANNUAL_PRICE_ID=
STRIPE_COUPON_SAVE20=
STRIPE_COUPON_WELCOME20=
STRIPE_COUPON_PROMO10=
STRIPE_COUPON_HALFPRICE=

# ---------- Resend / Email (leave empty to log to console) ----------
RESEND_API_KEY=
RESEND_FROM_EMAIL=alerts@statusenzin.me

# ---------- Monitor Engine ----------
MONITOR_CHECK_INTERVAL_SECONDS=30
MONITOR_HTTP_TIMEOUT_SECONDS=15

# ---------- Seeding ----------
# Set to true to seed demo data (status page, monitors, incidents) on first boot
SEED_DEMO_DATA=false
